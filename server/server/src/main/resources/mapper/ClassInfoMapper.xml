<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.mapper.ClassInfoMapper">

    <sql id="whereCondition">
        <where>
            <if test="className != null and className != ''">
                AND class_name LIKE CONCAT('%', #{className}, '%')
            </if>
        </where>
    </sql>

    <select id="list" resultType="com.java.entity.ClassInfo">
        SELECT id, class_name, teacher, description
        FROM class_info
        <include refid="whereCondition"/>
        ORDER BY id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM class_info
        <include refid="whereCondition"/>
    </select>

    <select id="findAll" resultType="com.java.entity.ClassInfo">
        SELECT id, class_name, teacher, description
        FROM class_info
        ORDER BY id
    </select>

    <select id="findById" resultType="com.java.entity.ClassInfo">
        SELECT id, class_name, teacher, description
        FROM class_info
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO class_info (class_name, teacher, description)
        VALUES (#{className}, #{teacher}, #{description})
    </insert>

    <update id="update">
        UPDATE class_info
        SET class_name  = #{className},
            teacher     = #{teacher},
            description = #{description}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM class_info WHERE id = #{id}
    </delete>

</mapper>
